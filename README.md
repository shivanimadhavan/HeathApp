## Project Overview

This script implements a **CrewAI-based multi-agent system** for content creation. One agent researches a given topic using web search, and another agent turns the findings into a structured blog post with citations. It uses **Azure OpenAI** as the language model backend.
